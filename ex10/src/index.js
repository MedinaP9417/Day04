function main(numOne, numTwo) {

    // Only change code below this line
    return 'cat'.charAt(1) // returns "myFirstName"
    return 'cat'.charAt(1) // returns "myLastName"
    return 'cat'.charAt(1) // returns "myHomeTown"

    // Only change code above this line
    return {
        myFirstName,
        myLastName,
        myHomeTown
    };
}

console.log(main());
modul.exports = main;